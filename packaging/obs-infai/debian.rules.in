#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_install:
	install -d debian/infai/usr/bin
	case "$(DEB_HOST_GNU_CPU)" in \
	  x86_64) binary=infai-amd64 ;; \
	  aarch64) binary=infai-arm64 ;; \
	  *) echo "Unsupported architecture: $(DEB_HOST_GNU_CPU)" >&2; exit 1 ;; \
	esac; \
	install -m 0755 "$$binary" debian/infai/usr/bin/infai
